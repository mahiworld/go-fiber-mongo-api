# go-fiber-mongo-api

## Api involving go-fiber with mongodb
## Basic CURD operations



## To use it:
1. Clone & Go inside directory 
    `git clone <repo-url>`
    `cd <dir>`

2. Run Api
    `go run main.go`